name: Publish to PyPI

on:
  release:
    types: [published]

jobs:
  build:
    name: Build distribution
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Install build tools
        run: pip install build

      - name: Build package
        run: python -m build

      # Das Gate prüft das gebaute Artefakt, nicht die Quelldateien — und es
      # steht vor dem Upload, weil danach nichts mehr korrigierbar ist: Eine
      # PyPI-Version ist unveränderlich, ein Fehler kostet einen
      # Versionssprung. Geprüft wird, was erst nach einem erfolgreichen Upload
      # auffiele: der `mcp-name`-Marker in der Wheel-METADATA (ohne ihn kann
      # die Registry die PyPI-Ownership nicht belegen), `server.json`
      # description ≤ 100 Zeichen (sonst 422 von der Registry) und dass der Tag
      # auf die Version zeigt, die tatsächlich gebaut wurde.
      - name: Release-Gate (gebaute Artefakte prüfen)
        run: python scripts/check_release_artifacts.py --dist dist --tag "${{ github.ref_name }}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/zh-education-mcp/
    permissions:
      id-token: write

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  publish-mcp:
    name: Publish to MCP Registry
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # required for MCP Registry Trusted Publishing (OIDC)
      contents: read

    steps:
      - uses: actions/checkout@v7

      - name: Sync server.json version to the release tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          jq --arg v "$VERSION" '.version = $v | .packages[0].version = $v' \
            server.json > server.tmp && mv server.tmp server.json

      - name: Install mcp-publisher
        run: |
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/').tar.gz" | tar xz mcp-publisher

      - name: Authenticate to MCP Registry (GitHub OIDC)
        run: ./mcp-publisher login github-oidc

      - name: Publish server to MCP Registry
        run: ./mcp-publisher publish
